import 'package:flutter/material.dart';
import 'package:glow/theme.dart';

class UnderlineWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(height: 2, width: 64, color: primaryColor);
  }
}
